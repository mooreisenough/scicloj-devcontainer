#!/bin/bash -xe

### run APL setup for RIDE IDE
/entrypoint

## Setup Clojure Dependencies and limit threads to avoid write lock error
clj -Sthreads 1 -P

## set up poetry 
rm poetry.lock

mkdir -p .venv && poetry install

clj -M:julia &
clj -M:rrr &